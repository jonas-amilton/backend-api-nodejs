generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Video {
  id          String   @id @default(uuid())
  title       String
  description String?
  duration    Int
  createdAt   DateTime @default(now()) @map("created_at")

  @@map("videos")
}